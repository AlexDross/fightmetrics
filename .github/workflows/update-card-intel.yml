name: Update Card Intel

on:
  schedule:
    - cron: '0 14 * * *'   # Daily 14:00 UTC
  workflow_dispatch:

jobs:
  update-card-intel:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout fightmetrics
        uses: actions/checkout@v4

      - name: Trigger Vercel redeploy
        run: curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"
