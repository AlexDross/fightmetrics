ufc_event_details.csv
node_modules/
build/
.DS_Store
__pycache__/
.claude/settings.local.json

# Foundation: candidate captures are disposable; the Stage 0 reference is committed
baseline/candidates/

# Foundation Stage 2 -- large regenerable backtest artifacts.
# NOT deleted; they stay on disk. backtest_results.json and
# backtest_results_original.json are deliberately NOT ignored -- BASELINE_NOTES.md
# treats those as the authoritative 60.71% record.
backtest_ledger.json
backtest_results_age.json
backtest_results_momentum.json

# Regenerable dry-run output of patch_atd.py:
#   python3 patch_atd.py --out src/fightersData_test.js
# Intentional artifact, never imported by the app. Kept on disk, not tracked.
src/fightersData_test.js
